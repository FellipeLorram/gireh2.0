import styled from "styled-components";

export const LabelWrapper = styled.label`
  width: 100%;
  p {
    color: var(--gray-100);
    font-size: var(--font-size-m);
    padding: 0 5px;
    margin-bottom: 8px;
  }
`;
